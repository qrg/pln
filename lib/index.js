'use strict';

/*
 const fs = require('fs');
 const yaml = require('js-yaml');
 const ect = require('ect');

 const cwd = process.cwd();
 const renderer = ect({root: `${cwd}/projects/sample-project/src/templates`});
 const yml = `${cwd}/projects/sample-project/src/contents/index.yaml`;
 const output = `${cwd}/projects/sample-project/dist/index.html`;

 try {
 const content = yaml.safeLoad(fs.readFileSync(yml, 'utf8'));
 const html = renderer.render('index.ect', content);
 fs.writeFileSync(output, html, 'utf8');
 console.log(html);
 } catch (err) {
 console.error(err);
 }
 */
