'use strict';

/*
const dirs = require('./lib/dirs');
const src = require('./lib/dirs').src;
const dist = require('./lib/dirs').dist;

console.log(dist('a', 'b', 'c'));
console.log(dirs.categories());
console.log(dirs.projects());
*/

/*
const Projects = require('./lib/projects');
const projects = new Projects();
console.log(projects.find({name: 'sample2'}));
*/

module.exports = (args) => {
  console.log(args);
};
